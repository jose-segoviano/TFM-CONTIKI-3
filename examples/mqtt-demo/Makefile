DEFINES+=PROJECT_CONF_H=\"project-conf.h\"
CONTIKI_PROJECT = mqtt-demo
all: $(CONTIKI_PROJECT)

CONTIKI = ../..

CONTIKI_WITH_IPV6 = 1
CONTIKI_WITH_RPL = 1
LAST_UPDATE_LIMIT = 200
RPL_CONF_DIO_REDUNDANCY = 10
RPL_CONF_DIO_INTERVAL_MIN = 1
RPL_CONF_DIO_INTERVAL_DOUBLINGS = 1

APPS += mqtt

include $(CONTIKI)/Makefile.include

